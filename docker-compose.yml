version: "3"
services:
  streaming:
    image: orcasound/orcanode:auto_streaming
    build: ./
    command: /sbin/my_init -- ./stream.sh
    restart: always
    env_file: .env
    ports:
      - "1234:1234"
      - "8080:8080"
    devices:
      - "/dev/snd:/dev/snd"
    privileged: true